[project]
name = "second-brain-worker"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "psycopg[binary]>=3.2",
    "ai-core",
]

[tool.uv.sources]
ai-core = { workspace = true }

[tool.uv]
package = false
