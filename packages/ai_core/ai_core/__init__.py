"""Shared AI pipeline and agent code, used by both the FastAPI service
and the background worker. Phase 1's capture pipeline lives in
pipeline.py; Phase 3 adds the six agent graphs alongside it."""

__version__ = "0.1.0"
